{
    "commands":  [
                     "if sudo test -s /etc/cloudmart/dashboard.env; then echo SECRET_FILE_EXISTS; else echo SECRET_FILE_EMPTY_OR_MISSING; fi",
                     "sudo systemctl show cloudmart-dashboard -p Environment"
                 ]
}
